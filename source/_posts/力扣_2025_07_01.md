---
title: 力扣2025_07_01
main_color: "#c0e0e0"  
categories: 力扣  
tags:
  - 力扣  
cover: https://free.picui.cn/free/2026/03/28/69c7f7c9b305a.png
---

## 📅 日期：
2025_07_01

## 📌 今日题目：
盛最多水的容器

### 📝 题目描述

给定一个长度为 n 的整数数组 height 。有 n 条垂线，第 i 条线的两个端点是 (i, 0) 和 (i, height[i]) 。

找出其中的两条线，使得它们与 x 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

---

### 示例  

**示例 1：**  
输入：`height = [1,8,6,2,5,4,8,3,7]`  
输出：`49`  
解释：选择第 2 条（高度为 8）和第 9 条（高度为 7）的线，容器容量为 min(8,7) * (8-1) = 49。

**示例 2：**  
输入：`height = [1,1]`  
输出：`1`

---

## 思路分析

本题的核心是**双指针法**，利用贪心思想。

- 容器的容量由两条线中较短的那一条和它们之间的距离决定。
- 设定两个指针，分别指向数组的两端。
- 每次计算当前两指针间的容量，并更新最大值。
- 然后移动较短的那一侧指针（因为移动较长的那一侧不会增加容量）。
- 直到左右指针相遇。

**时间复杂度：** O(n)

---

## ✅ Python 解法
```python
def max_area(height):
    left, right = 0, len(height) - 1
    max_water = 0
    while left < right:
        h = min(height[left], height[right])
        max_water = max(max_water, h * (right - left))
        if height[left] < height[right]:
            left += 1
        else:
            right -= 1
    return max_water
```

## ✅ Java 解法
```java
class Solution {
    public int maxArea(int[] height) {
        int left = 0, right = height.length - 1;
        int maxWater = 0;
        while (left < right) {
            int h = Math.min(height[left], height[right]);
            maxWater = Math.max(maxWater, h * (right - left));
            if (height[left] < height[right]) {
                left++;
            } else {
                right--;
            }
        }
        return maxWater;
    }
}
```

## 🧠 今日收获
掌握了双指针法在区间类最值问题中的应用，理解了贪心思想如何优化暴力解法。
