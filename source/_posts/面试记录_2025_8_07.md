---
title: 面试记录_2025_8_07
main_color: "#F3E0C4"
categories: 面试官
date: 2025-08-07
tags:
  - Java
  - Python
cover: https://free.picui.cn/free/2026/03/28/69c74d5a9ef14.png
---


# 面试记录 2025-8-07

## 📌 候选人信息
- **姓名**：xxx  
- **技术方向**：Java后端开发  
- **工作年限**：6年  

## 自我介绍环节（5分钟）
请候选人用2分钟简述自己的技术背景、核心技能

---


### 八股

### 八股文整理及参考答案

1. **自我介绍**
   - **问题**：请简要介绍一下你自己，重点突出你在技术领域的经验和成就。

2. **HashMap为什么线程不安全？**
   - **参考答案**：HashMap不是线程安全的主要原因是其内部结构在多线程环境下可能会导致数据不一致或循环链表的问题，特别是在扩容时。解决方法包括使用ConcurrentHashMap或Collections.synchronizedMap()来保证线程安全。

3. **线程run和start的区别？**
   - **参考答案**：`start()`方法用于启动一个新线程并执行`run()`方法中的代码；而直接调用`run()`方法则是在当前线程中执行，不会启动新的线程。因此，要创建并启动新线程，必须使用`start()`。

4. **为什么平时都是使用线程池创建线程，直接new一个线程不好吗？**
   - **参考答案**：直接new线程会导致频繁创建和销毁线程，增加了系统的开销。线程池通过重用线程减少了这种开销，同时可以控制最大并发数，避免资源耗尽。

5. **execute和submit的区别**
   - **参考答案**：`execute()`方法仅执行任务，不返回结果；而`submit()`不仅可以执行任务，还可以返回一个Future对象，用于获取异步执行的结果。

6. **MySQL与Redis如何保证数据一致性？**
   - **参考答案**：通常采用双写策略，在更新数据库的同时也更新缓存；或者使用消息队列确保操作的顺序性。也可以设置合理的过期时间，让缓存自动失效后重新加载最新数据。

7. **ThreadLocal内存泄漏的原因？ ThreadLocal使用场景有哪些？**
   - **参考答案**：内存泄漏主要由于ThreadLocal变量持有对外部类的强引用，当线程长时间存活时可能导致内存无法释放。常用场景包括存储用户会话信息、事务管理等需要在线程生命周期内保持的数据。

8. **索引实效的几种情况**
   - **参考答案**：包括但不限于：查询条件未使用索引字段、隐式类型转换、函数作用于索引字段、使用了OR条件等。

9. **MySQL查询limit 1000,10 和limit 10速度一样快吗？ 深分页如何优化**
   - **参考答案**：不一样，`LIMIT 1000,10`需要跳过前1000条记录，效率较低。优化方法包括使用覆盖索引、延迟关联、基于主键范围查询等。

10. **自增主键一定是连续的吗？**
    - **参考答案**：不一定，例如在发生回滚、删除记录或服务器重启的情况下，自增主键可能出现不连续的情况。

11. **Transactional 失效的情况**
    - **参考答案**：常见原因包括：方法非public修饰、异常被捕获并未抛出、方法内部调用其他私有方法上的事务注解等。正确配置事务传播行为也很关键。

12. 

### 大模型项目相关问题

1. **Nacos配置管理案例**：你能分享一下在《国元投行大模型平台》项目中使用Nacos进行配置管理的具体案例吗？比如，是如何实现动态刷新配置而不重启服务的？
2. **定时调度模式和断点续传**：在这个项目中，你是如何设计定时调度模式以同步底稿系统的文件？针对超大文件的断点续传，具体实现了哪些策略？
3. **队列阻塞问题**：在使用RocketMQ时，你是如何避免或解决队列阻塞问题的？
4. **在线编辑后保存、修改问题**：对于腾讯文档在线编辑功能，你是如何确保数据的一致性和正确性？
5. **拆分、检索、溯源**：在问答模块中，你是如何处理用户输入的问题拆分、检索以及答案溯源的？
6. **结构化文件处理流程**：请描述一下在处理不同类型的文件时，你的处理流程是怎样的？特别是涉及到结构化数据存储的设计。
7. **数据存储方案**：在该项目中，你们是如何选择和设计数据存储方案（MySQL, Redis, Elasticsearch等）的？
8. **WebSocket vs SSE**：为什么在实时渲染页面时选择了WebSocket而不是Server-Sent Events (SSE)？
9. **消息队列回滚机制**：当需要对消息队列中的操作进行回滚时，你们是如何设计和实现这一机制的？
10. **限流规则设计**：请详细说明在该项目中，你是如何设计限流规则来适应不同的业务场景的？

### 量子计算云平台项目相关问题

1. **权限控制**：请解释一下在《中国电信“天衍”量子计算云平台》项目中，你是如何实施权限控制的？
2. **服务部署经验**：分享一下你在该项目中使用Docker和Kubernetes进行服务部署的实际操作经验，遇到了哪些挑战？
3. **高并发处理**：面对高并发请求时，你采取了哪些措施保证系统的稳定性和响应速度？可以举例说明Sentinel限流机制的应用。
4. **数据库字段更新**：当你需要在数据库中新增字段时，是如何进行版本更新的，以确保不影响现有功能？




### 场景题

#### 1. 单点登录：多个项目如何打通登陆
- **问题描述**：
  如何在多个不同的项目或服务之间实现单点登录（SSO），使得用户只需登录一次，就可以访问所有相互信任的应用程序？请详细说明你的设计方案，包括技术选型、实施步骤以及可能遇到的问题及解决方案。

- **参考回答方向**：
  - **技术选型**：可以选择OAuth2.0、SAML或者JWT等标准协议来实现SSO。OAuth2.0因其灵活性和广泛的支持成为首选。
  - **实施方案**：
    - 建立一个中心化的认证服务器，所有应用都通过该服务器进行身份验证。
    - 使用令牌机制（如JWT）存储用户会话信息，避免传统session带来的跨域问题。
    - 在客户端和服务端设置适当的过期时间和刷新策略，确保安全性的同时不影响用户体验。
  - **挑战与解决方案**：
    - **安全性**：确保令牌的安全传输和存储，使用HTTPS加密通信，对敏感数据进行加密处理。
    - **性能优化**：对于大型系统，考虑引入分布式缓存（如Redis）来加速令牌验证过程。

#### 2. 新技术应用：假如重构，你作为项目负责人，从什么方面入手
- **问题描述**：
  如果需要对现有的老旧系统进行重构，你会从哪些方面开始着手？请结合具体的技术栈和个人经验，谈谈你的重构策略，包括但不限于代码质量提升、架构优化、测试覆盖等方面。

- **参考回答方向**：
  - **现状评估**：首先进行全面的系统分析，了解当前系统的架构、依赖关系、存在的瓶颈和技术债务。
  - **目标设定**：明确重构的目标，比如提高可维护性、增强扩展性或是改善性能表现。
  - **分阶段实施**：
    - **模块化改造**：将原有紧密耦合的代码逐步拆分为独立模块，便于后续迭代更新。
    - **技术升级**：根据最新的技术趋势选择合适的框架和技术栈，如升级Spring Boot版本以利用其新特性。
    - **自动化测试**：增加单元测试、集成测试覆盖率，确保重构过程中功能不受影响。
    - **持续集成/部署(CI/CD)**：引入CI/CD流程，加快开发周期，提高部署效率。
  - **风险管理**：制定详细的回滚计划，确保在出现问题时可以迅速恢复到旧版本状态；同时保持与利益相关者的沟通透明度，及时汇报进展和潜在风险。


## 算法题

https://leetcode.cn/problems/subarray-sum-equals-k/?envType=study-plan-v2&envId=top-100-liked



## 第二位候选人


### 智能运维平台相关问题
1. 你提到使用了Redis和RabbitMQ来解决高并发下的数据处理问题。请详细解释你是如何设计消息队列的消息持久化策略以保证数据不丢失？在极端情况下（如系统崩溃），如何恢复未处理完的数据？
2. 关于使用FFmpeg+JavaCV处理大视频文件的问题，具体描述一下你是如何实现视频分段上传的技术细节？包括如何确保分段上传后视频的完整性和一致性？

### 单兵作业辅助系统相关问题
1. 对于安卓开发的学习过程，请分享你是如何在短时间内掌握所需技能并成功应用于项目的？
2. 你是如何通过Websocket+心跳机制保持头盔与后端服务之间的长连接的？
3. 在没有固定IP的情况下，如何实现向单兵设备（如头盔）推送消息的功能？

### 用户中心+运行监测开发相关问题
1. 你能详细解释一下JWT在前后端分离架构中的作用原理吗？特别是在用户信息变更的情况下，如何安全地更新Token中的信息？
3. 在搭建Neo4j数据库并导入图谱数据的过程中，如何导入，图谱数据从哪来的，数据是什么
4. 基于RADB模型进行细粒度权限控制的设计中，你是如何处理角色和权限之间关系的动态变化？例如，当新添加一个权限时，如何高效地更新受影响的角色权限列表？


### 科华网上订单平台相关问题
1. 在这个完全由你负责的项目中，你是如何进行代码版本控制和质量保证的？


### 洽洽营销集成平台相关问题
1. 使用Redis优化系统性能的过程中，你是如何决定哪些数据需要缓存，以及缓存的有效期是如何设定的？能否分享一个具体的案例说明通过Redis优化带来的性能提升效果？
2. 对于活动提报审批流程的设计，你采取了哪些措施来确保审批过程的安全性和效率？例如，如何防止重复提交或非法操作？





### 智能运维平台相关问题

1. **消息队列持久化策略**：在设计RabbitMQ的消息持久化时，我们确保所有重要的消息都设置了`delivery_mode=2`（持久化模式），并使用了持久化的队列和交换机。对于极端情况下的数据恢复，我们实现了定期的备份机制，并且利用数据库记录消息的状态以便在系统重启后重新发送未完成的消息。
   
   - **参考答案**：通过设置消息、队列和交换机为持久化模式，并结合数据库记录状态进行恢复。

2. **视频分段上传技术细节**：我们首先使用FFmpeg将大视频文件分割成多个小片段，然后通过JavaCV处理这些片段以确保其格式一致性。每个片段都有一个唯一的标识符，上传后通过后台服务合并这些片段，并验证最终文件的完整性。

   - **参考答案**：采用FFmpeg分割视频，JavaCV处理格式一致性，上传过程中保持唯一标识符用于后续合并及完整性检查。

### 单兵作业辅助系统相关问题

1. **安卓开发学习过程**：通过观看在线教程、阅读官方文档以及实际编码测试，逐步掌握了Android开发的基本技能。重点学习了Android SDK中的核心组件和服务，如广播接收器、内容提供者等。

   - **参考答案**：利用在线资源自学，专注于Android SDK的核心组件和服务的学习。

2. **Websocket+心跳机制**：通过在客户端和服务器端之间建立Websocket连接，并定期发送心跳包来维持连接状态。如果一段时间内没有收到心跳信号，则认为连接已断开，需要重新建立连接。

   - **参考答案**：基于Websocket协议，定期发送心跳包检测连接状态，适时重建连接。

3. **推送消息到无固定IP设备**：通过使用动态DNS服务或云服务提供商提供的API获取当前IP地址，结合长连接机制实现消息推送。

   - **参考答案**：利用动态DNS服务更新IP地址信息，结合长连接机制实现消息推送。

### 用户中心+运行监测开发相关问题

1. **JWT作用原理**：JWT是一种开放标准（RFC 7519），用于在网络应用环境间传递声明。用户登录成功后，服务器生成包含用户信息的JWT令牌返回给客户端。在用户信息变更时，可通过重新签发新的JWT令牌来更新Token中的信息。

   - **参考答案**：解释JWT的工作原理及其在用户信息变更时如何更新Token。

2. **Neo4j图谱数据导入**：图谱数据通常来源于业务需求分析，例如从现有的关系型数据库中提取数据并转换为适合Neo4j存储的形式。导入过程涉及编写脚本或使用ETL工具自动化此过程。

   - **参考答案**：说明数据来源及转换过程，强调使用脚本或ETL工具进行数据导入。

3. **角色权限动态管理**：当添加新权限时，通过查询与该权限相关的角色列表，并更新这些角色对应的权限集合。可以考虑使用缓存机制提高查找效率。

   - **参考答案**：介绍查询相关角色并更新权限集合的方法，提及可能用到的缓存机制。

### 科华网上订单平台相关问题

1. **代码版本控制和质量保证**：使用Git进行版本控制，遵循GitFlow工作流。代码审查、单元测试、集成测试是确保代码质量的关键步骤。

   - **参考答案**：描述GitFlow工作流的应用，强调代码审查、单元测试的重要性。

2. **EasyUI框架最佳实践**：合理规划页面布局，减少不必要的组件加载，优化图片和样式资源的加载顺序，提升用户体验。

   - **参考答案**：分享关于页面布局优化、资源加载顺序调整的最佳实践。

### 洽洽营销集成平台相关问题

1. **Redis缓存策略**：根据数据访问频率和时效性决定是否缓存。有效期设定依据业务逻辑，如促销活动的有效期。

   - **参考答案**：解释如何根据数据特点设定缓存策略及其有效期。

2. **审批流程安全措施**：采用唯一ID标记每项提交，服务器端校验防止重复提交；对关键操作实施二次确认或验证码机制。

   - **参考答案**：详细介绍防止重复提交和非法操作的具体措施。 




基于陈姚志参与的多个项目，我们可以总结出一些通用的技术面试问题，这些问题旨在评估他的技术深度、解决问题的能力以及对项目的整体理解。以下是几个通用的问题：

### 技术深度与架构设计
1. **分布式系统的设计与优化**：请描述你在设计和优化分布式系统时的经验，特别是如何解决高并发、高可用性和数据一致性等问题？
   - **参考答案**：应提及使用的技术栈（如Spring Cloud、Kubernetes等），并具体说明在实际项目中是如何实现负载均衡、服务发现、限流降级等功能的。

2. **性能调优经验**：你有哪些关于JVM性能调优或数据库性能调优的实际案例？在面对性能瓶颈时，你是如何定位问题并实施解决方案的？
   - **参考答案**：可以分享具体的调优措施，比如调整GC策略、优化SQL查询、使用索引等。

3. **微服务架构下的部署与运维**：在你的项目中，你是如何利用Docker、Jenkins和Kubernetes等工具进行自动化部署和持续集成/交付的？
   - **参考答案**：详细介绍CI/CD流程的设计思路及其实现方式，包括容器化部署的具体步骤。

### 解决问题的能力
4. **应对突发情况的策略**：在项目开发过程中遇到的最大挑战是什么？你是如何克服这些挑战的？
   - **参考答案**：提供具体的例子，展示其分析问题、制定解决方案的能力。

5. **新技术的学习与应用**：当面临一个新的技术领域时（例如从零学习安卓开发），你是如何快速掌握所需技能并将之应用于实际项目的？
   - **参考答案**：强调自学能力、资源利用（如在线课程、文档）以及实践的重要性。

### 项目管理与团队协作
6. **项目管理和团队领导**：作为开发组长，你是如何管理项目进度、协调团队成员之间的合作以及确保项目按时高质量完成的？
   - **参考答案**：讨论敏捷开发方法的应用、任务分配策略、定期会议的作用等。

7. **跨部门沟通技巧**：在与非技术人员（如产品经理、设计师）合作时，你是如何有效地传达技术限制和可能性，以达成共识的？
   - **参考答案**：说明如何通过简化术语、制作原型等方式促进理解。

### 持续改进与个人成长
8. **个人博客与知识分享**：你能谈谈为什么选择写博客，并分享一些博客中的内容吗？这对你个人职业发展有何帮助？
   - **参考答案**：解释写博客的目的（如记录学习过程、帮助他人），并举例说明某些文章的主题及其影响。

这些问题不仅涵盖了技术层面的知识点，还关注了候选人在实际工作中的软技能表现，有助于全面了解候选人的能力和潜力。





https://leetcode.cn/problems/subarray-sum-equals-k/?envType=study-plan-v2&envId=top-100-liked

