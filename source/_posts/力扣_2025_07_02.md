---
title: 力扣2025_07_02
main_color: "#c0e0e0"  
categories: 力扣  
tags:
  - 力扣  
cover: https://free.picui.cn/free/2026/03/28/69c7f7c9b305a.png
---

## 📅 日期：
2025_07_02

## 📌 今日题目：
三数之和

### 📝 题目描述

给你一个包含 n 个整数的数组 `nums`，判断 `nums` 中是否存在三个元素 a、b、c ，使得 a + b + c = 0？请你找出所有和为 0 且不重复的三元组。

**注意：** 答案中不可以包含重复的三元组。

---

### 示例  

**示例 1：**  
输入：`nums = [-1,0,1,2,-1,-4]`  
输出：`[[-1,-1,2],[-1,0,1]]`

**示例 2：**  
输入：`nums = []`  
输出：`[]`

**示例 3：**  
输入：`nums = [0,0,0]`  
输出：`[[0,0,0]]`

---

## 思路分析

本题的核心是**排序 + 双指针**。

- 首先对数组进行排序，便于去重和双指针操作。
- 固定第一个数 `nums[i]`，在其右侧用双指针查找另外两个数，使三数之和为 0。
- 若和大于 0，右指针左移；若和小于 0，左指针右移。
- 注意跳过重复元素，避免重复三元组。

**时间复杂度：** O(n^2)

---

## ✅ Python 解法
```python
def three_sum(nums):
    nums.sort()
    res = []
    n = len(nums)
    for i in range(n):
        if i > 0 and nums[i] == nums[i-1]:
            continue  # 跳过重复
        left, right = i + 1, n - 1
        while left < right:
            s = nums[i] + nums[left] + nums[right]
            if s == 0:
                res.append([nums[i], nums[left], nums[right]])
                left += 1
                right -= 1
                while left < right and nums[left] == nums[left-1]:
                    left += 1
                while left < right and nums[right] == nums[right+1]:
                    right -= 1
            elif s < 0:
                left += 1
            else:
                right -= 1
    return res
```

## ✅ Java 解法
```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Arrays.sort(nums);
        List<List<Integer>> res = new ArrayList<>();
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            if (i > 0 && nums[i] == nums[i-1]) continue; // 跳过重复
            int left = i + 1, right = n - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];
                if (sum == 0) {
                    res.add(Arrays.asList(nums[i], nums[left], nums[right]));
                    left++;
                    right--;
                    while (left < right && nums[left] == nums[left-1]) left++;
                    while (left < right && nums[right] == nums[right+1]) right--;
                } else if (sum < 0) {
                    left++;
                } else {
                    right--;
                }
            }
        }
        return res;
    }
}
```

## 🧠 今日收获
掌握了排序+双指针解决三数之和问题的套路，理解了如何高效去重和优化暴力解法。


